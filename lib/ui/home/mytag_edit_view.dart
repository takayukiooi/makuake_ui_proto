import 'package:flutter/material.dart';

class MyTagEditView extends StatelessWidget {
  const MyTagEditView({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
