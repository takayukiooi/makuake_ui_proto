import 'package:flutter/material.dart';

class MyTagView extends StatelessWidget {
  const MyTagView({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('MyTagView'),
    );
  }
}
